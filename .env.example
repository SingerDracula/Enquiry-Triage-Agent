# Optional real-model configuration. The project is runnable without these values
# by using demo-fast and demo-conservative providers.
OPENAI_COMPATIBLE_BASE_URL=https://api.deepseek.com/v1
OPENAI_COMPATIBLE_API_KEY=replace_with_your_key

# Optional costs in USD per million tokens. These are configuration rather than
# hard-coded facts, so reports retain the price used at run time.
OPENAI_COMPATIBLE_INPUT_USD_PER_MILLION=0
OPENAI_COMPATIBLE_OUTPUT_USD_PER_MILLION=0
